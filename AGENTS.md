# Project rules

- Do not run `flutter analyze`.
- Do not run `flutter build`.
- Do not run `flutter test`.
- Do not run any Gradle command.
- Only edit code files when requested.
- After editing, summarize changed files and tell me which commands I should run manually.
- If validation is needed, ask me before running any command.

# Vietnamese text rule

- All Vietnamese text MUST use valid UTF-8 encoding.
- NEVER output corrupted Vietnamese text or mojibake.
- If any Vietnamese text is malformed, fix it before completing the task.
- Final output and source code must display Vietnamese correctly.
- Never replace Vietnamese text with escaped unicode or broken encoding.